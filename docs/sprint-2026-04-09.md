# Sprint Report — April 9, 2026

## Session Overview

Full production-hardening pass on the IMPerfect codebase. Started with VibeStack setup and squad generation, then worked through 22 engineering tasks across security, reliability, testing, performance, observability, and features.

## Infrastructure Setup

### VibeStack
- Updated `CLAUDE.md` with Skills section, fixed `make test` description
- Cleaned up `README.md` — removed create-next-app boilerplate, categorized all Makefile commands
- Created `.claude/skills/` with 7 skill files (vibestack, cli-first, docs, todo, squad, lsp, bosskey)

### Squad (Domain Rules)
- Generated 5 domain rule files in `.claude/rules/`: auth, public-site, team-hub, data-layer, management
- Created 1 specialist subagent: `data-layer-specialist` (for DB schema/migration changes)
- Manifest at `.claude/squad.json`

## Completed Tasks (22)

### Security & Data Integrity (6)
1. **Try-catch error handling** — All 19 API route handlers across 17 files now catch exceptions and return consistent 500 errors via `apiError()`
2. **CSRF origin verification** — `verifyCsrfOrigin()` added to every POST/PATCH/DELETE handler (18 files), including public endpoints
3. **Rate limiting on public endpoints** — `checkRateLimit()` added to POST `/api/scrim-applications` and POST `/api/community-teams`
4. **Enum validation** — Created `invalidEnum()` in `validate.ts`. Applied to tournaments (game, status), sponsors (tier, status), revenue (category, payment_method)
5. **Max-length validation** — Character limits on all user-submitted strings in scrim applications and community teams. Max 20 players per team submission.
6. **`.env.example` updated** — Added `OWNER_DISCORD_ID`, `DISCORD_BOT_TOKEN`, and 4 `DISCORD_CHANNEL_*` variables

### Error Handling & Reliability (3)
7. **Error boundaries** — Created `src/app/[locale]/error.tsx` and `src/app/scrims/error.tsx` matching existing Team Hub pattern
8. **Discord notification logging** — Replaced 7 silent `.catch(() => {})` with descriptive `console.error` logging
9. **Dynamic division list** — Cron reminders now query active divisions from `players` table instead of hardcoded `["IMPerfect", "Shadows", "Echoes"]`

### Testing (5)
10. **E2E: scrim application** — 5 Playwright tests (form render, validation errors, checkbox validation, successful submit with API mock, duplicate error handling)
11. **E2E: community registration** — 2 Playwright tests (page render, navigation from community page)
12. **Unit: permissions** — 65 tests covering `resolveOrgRole`, `isOwner`, `hasRole`, and all `can.*` methods across all 6 roles
13. **Unit: admin API** — 17 tests for users GET/PATCH, invites GET/POST, audit-log GET (auth, permissions, happy paths)
14. **Unit: availability resolution** — 6 tests verifying override > template > unset priority and day-of-week mapping

**Test count: 26 → 114 passing (4 pre-existing failures in api-players and api-availability tests)**

### Performance (4)
15. **Database indexes** — New migration `20260409_performance_indexes.sql` with 6 indexes: `players(division)` filtered, `availability(week_start, player_discord_id)`, `schedule_blocks(week_start, division)`, `scrims(division, scheduled_at)`, `availability_templates(player_discord_id)`, `availability_overrides(player_discord_id, override_date)`. Also updated `supabase-schema.sql`.
16. **Loading skeletons** — Created `loading.tsx` for 5 Team Hub pages: roster, scrims, announcements, admin, community
17. **Image optimization** — Converted 8 player profile images from PNG to WebP: **19MB → 780KB (96% reduction)**. Updated `RosterTeaser.tsx` references, deleted old PNGs.
18. **next/image conversion** — Replaced the last raw `<img>` tag in `PlayerStatsSection.tsx` with `next/image` `<Image>` component

### Observability (2)
19. **Sentry configs** — Enhanced `sentry.server.config.ts` and `sentry.edge.config.ts` with environment tagging via `VERCEL_ENV`
20. **Structured request logging** — Added `logRequest()` to 7 admin and management mutation routes (POST/PATCH/DELETE handlers)

### Features (2)
21. **Pre-resolved availability API** — New endpoint `GET /api/availability/resolved?division=X&weeks=4` returns resolved availability (override > template > unset) for all players in a division. MANAGER+ only. 5-minute cache headers. Used by scrim scheduling overlay.
22. **"Schedule Scrim" action** — Added "Schedule Scrim" button to accepted scrim applications in `ScrimApplicationsPanel`. Clicking it switches to the scrims tab and pre-fills the creation form with the application's game, team name, and format.

### OG Images
- Created `src/app/opengraph-image.tsx` — dynamic OG image generator using Next.js `ImageResponse` (edge runtime). Renders branded card with #C8E400 on #0D0D0D.
- Added `twitter` card metadata and `siteName`/`locale` to root layout OG config.

## Remaining Tasks (needs human input)

| Task | Blocker |
|------|---------|
| MR username for the_mofn_ninja | Ask the player |
| MR username for tides100ping | Ask the player |
| OW2 roster data | Roster rebuilding |
| Echoes subteam roster | TBA |
| TikTok/YouTube in Footer | Need actual handles |
| Build merch store | Phase 3, needs design |

## Pre-existing Issues Found
- 4 failing unit tests in `api-players.test.ts` (3) and `api-availability.test.ts` (1) — mocks don't match the current `resolveOrgRole` + `can.*` permission system. These were broken before this session.
- TypeScript diagnostic: missing `CommunityTeamsPanel` import in one team-hub page
- TypeScript diagnostic: type error in `checkout.tsx` (likely in v0-perfume-store-clone, not this project)

## Files Changed (key)
- 30+ API route files (try-catch, CSRF, rate limiting, logging)
- `src/lib/validate.ts` — added `invalidEnum()`
- `src/lib/api-error.ts`, `src/lib/csrf.ts`, `src/lib/rate-limit.ts` — no changes, already existed
- `.env.example` — added 6 vars
- `supabase/migrations/20260409_performance_indexes.sql` — new
- `supabase-schema.sql` — updated with indexes
- 5 new `loading.tsx` files
- 8 player images converted PNG → WebP
- 4 new test files (95 new tests)
- 2 new E2E test files (7 tests)
- `src/app/opengraph-image.tsx` — new
- `src/app/[locale]/error.tsx`, `src/app/scrims/error.tsx` — new
- `src/app/api/availability/resolved/route.ts` — new
- `ScrimApplicationsPanel.tsx`, `ScrimsPanel.tsx` — Schedule action feature
- `.claude/rules/` (5 files), `.claude/agents/` (1 file), `.claude/squad.json` — new
- `.claude/skills/` (7 files) — new
